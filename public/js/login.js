document.getElementById('login-form').addEventListener('submit', function (e) {
  e.preventDefault();
  // TODO: replace with real API call once auth backend is ready
  window.location.href = '/home';
});
